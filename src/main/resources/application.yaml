spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/clinica_db
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    mail:
      host: sandbox.smtp.mailtrap.io
      port: 2525
      username: b1e179a89f11ff
      password: f0228d40e93793
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}

springdoc:
  swagger-ui:
    path: /swagger-ui.html